import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practicaSQLite004d',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
